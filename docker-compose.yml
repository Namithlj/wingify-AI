version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: fda_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

# Optional: add an app service if you want to containerize the FastAPI app
# app:
#   build: .
#   command: uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir financial-document-analyzer-debug
#   volumes:
#     - ./:/app
#   ports:
#     - "8000:8000"
#   depends_on:
#     - redis
